<%= t('password_reset.mailer.body.title') %>

<%= t('password_reset.mailer.body.instructions') %>

<%= link_to t('password_reset.mailer.body.link'), edit_password_reset_url(@user.reset_token,
                                                      email: @user.email) %>

<%= t('password_reset.mailer.body.instructions') %>